A bit more from May 30:
Look into how to get the route geometries of the routes (GeoJSON)?
Add more file export/import options. (I.e. export it directly as an HTML file)
Map rotation.

New stuff from Apr 7:
Display the round trip travel time
Set custom zoom to get the same shot every time
Put circular “buttons” on midpoints of sections. When clicked, a checkpoint should be inserted there (turn on checkpoint mode so the user can click on the position).
Use GTFS data to generate the coordinates. Input a series of routeIDs and bus numbers and turn it into a json file storing stations. Then, manually add the checkpoints. 
Change the rolling stock thingy (the images on the trains. The rolling stock are different for different lines.)

Editor:
- Make the map/JSON display/left editing page separate components?


To figure out how to do:
- Scheduling - How to upload schedules of transit services easily?
    - GTFS data? Use a python script to generate a json.
    - GeoJSON?


Stuff to do:
- Add z-indexes, allowing for users to put things on top of each other.
- Make the animation generator fetch a json directly (API), so no uploading is needed.
- New progress function for longer lines
- Refer to the mtr wiki on the number of trains operating on each line. Make sure it is consistent.
- Idea: Separate a "branch editor" from the "line editor" to reduce the number of lines??
- Add settings bar (e.g. hiding the lines, enabling/disabling certain lines in the video generator)
- Highlight lines when clicking a button

Ongoing:
Split the code into shorter readable segments

Less important:
Branch synchronisation: Maybe consider keeping the spawn frequency uniform for a branch? (i.e. modify it slightly such that the gap between each train is equivalent.)


Later stuff to consider:
- Interface for making new icons (just store images in a string?)